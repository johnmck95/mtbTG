import { render } from "@testing-library/react";

// test("tireInsertCalc() returns 0 for beginners", () => {
   
//   });

it ('tester test', () => {
    const a: string = 10;
    expect(a).toBe(10)
    // expect(5+5).toBe(10)
})